[package]
name = "buildfix-report"
version = "0.3.1"
publish = ["crates-io"]
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Core report projection for buildfix plan/apply outcomes."
repository.workspace = true
homepage.workspace = true
readme = "README.md"
keywords = ["cargo", "workspace", "report", "planning", "telemetry"]
categories = ["development-tools"]

[dependencies]
chrono.workspace = true
buildfix-receipts = { version = "0.3.1", path = "../buildfix-receipts" }
buildfix-types = { version = "0.3.0", path = "../buildfix-types" }
serde_json.workspace = true
